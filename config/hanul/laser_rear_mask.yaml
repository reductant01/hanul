lidar_scan_mask:
  ros__parameters:
    input_topic: /scan_raw
    output_topic: /scan
    # 현재 SLLIDAR 스캔 기준으로 로봇 뒤쪽 가려지는 구간이 0deg 근처라서
    # -60deg ~ +60deg 를 마스킹해 뒤쪽 120도를 /scan 에서 제외한다.
    # 필요 시 이 각도만 바꿔서 조정하면 된다.
    mask_lower_angle_deg: -60.0
    mask_upper_angle_deg: 60.0
