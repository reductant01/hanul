lidar_scan_mask:
  ros__parameters:
    input_topic: /scan_raw
    output_topic: /scan
    # 정면 0도를 기준으로 앞쪽 -120deg ~ +120deg 만 남긴다.
    # 즉 뒤쪽 120도(+120deg ~ +180deg, -180deg ~ -120deg)는 /scan에서 제외한다.
    keep_lower_angle_deg: -120.0
    keep_upper_angle_deg: 120.0
